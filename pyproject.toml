[project]
name = "deeplib"
version = "1.0.0"
authors = [{name = "Nick Seinsche", email = "nick.seinsche@gmail.com"}]
description = "A place for my deep learning studies and experiments."
license = {file = "LICENSE.txt"}
readme = "README.md"
keywords = ["machine", "learning", "deep", "learning", "neural", "networks", "artificial", "intelligence"]
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "idx2numpy",
    "matplotlib",
    "seaborn",
    "openpyxl",
    "plotly",
    "kaggle",
    "tqdm",
    "scikit-learn",
    "casadi",
    "typing-extensions>=4.15.0",
]

[project.optional-dependencies]
full = [
    "gensim",
    "spacy",
    "nltk",
    "tensorflow[and-cuda]"
]
dev = [
    "build",
    "pytest",
    "flake8",
    "flake8-docstrings"
]
docs = ["sphinx", "sphinx-rtd-theme"]

[tool.setuptools.packages.find]
where = ['.']
include = ['deeplib']
exclude = ['venv', 'tests', "ml_studies"]

[build-system]
requires = [
    "setuptools >= 40.9.0"
]
build-backend = "setuptools.build_meta"

[tool.black]
extend_exclude = '/(\.venv.*)/'
line-length = 88

# pyproject.toml
[tool.pytest.ini_options]
pythonpath = [".", "deeplib"]
